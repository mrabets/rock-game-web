<h1>Moves</h1>

<table class="responsive nowrap table table-hover" id="dttb-moves" data-sort="true" data-source="<%= url_for(format: :json)%>">
  <thead>
    <tr>
      <th data-data="content">Content</th>
      <th data-data="user">User</th>
      <th data-data="room">Room</th>
      <th data-data="url" data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export" width="100px"></th>
    </tr>
  </thead>

</table>

<br>

<%= link_to 'New Move', new_move_path %>